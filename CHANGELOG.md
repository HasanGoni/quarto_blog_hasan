# Changelog

All notable changes to this project are logged here, newest first.

## 2026-08-14
- Ignore local venv (`.venv-boogu/`) and model cache (`.model_cache/`) directories used by the boogu notebook.
- Update `_publish.yml`: Quarto Pub rewrote the destination URL as an unquoted scalar during publish.
- Add draft boogu semiconductor-defect image-editing notebook under `notebooks/_drafts/` (not yet published).
